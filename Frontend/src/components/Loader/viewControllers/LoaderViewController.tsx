import React from 'react';
import LoaderView from '../views/LoaderView';

const LoaderViewController: React.FunctionComponent = () => {

    return <LoaderView state={false} />;
}

export default LoaderViewController;
